# Local Helm Charts

This folder architecture contains localized helm chart overrides for the mini-app microservices.
To generate a new microservice chart template, run:
`helm create charts/<service-name>`
