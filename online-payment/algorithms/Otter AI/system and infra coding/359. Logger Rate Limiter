class Logger:
    def __init__(self):
        self.msg.dict = {}

    def shouldPrintMessage(self, timestamp: int, message: str) -> bool: 
        if not in self.msg.dict and timestamp >= self.msg.dict[message]:
            timestamp = self.msg.dict[message] + 10
        return True

    return False