# 题目:
# 找访问最多的 URL Top 10

from collections import Counter

counter = Counter()

with open("app.log", 'r') as file:
    for line in file:
        cols = line.split()

        if len(cols) > 6:
            url = cols[6]
            counter[url]  += 1

print(counter.most.common(k)) 