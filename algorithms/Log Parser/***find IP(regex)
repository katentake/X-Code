import re

# pattern = r"\d+\.\d+\.\d+\.\d+"
pattern = r"(?:\d{1,3}\.){3}\d{1,3}"
with open("app.log", 'r') as file:
    for line in file:
        match = re.search(pattern, line)

        if match:
            print(match.group())