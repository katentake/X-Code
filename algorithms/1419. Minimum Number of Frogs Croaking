class Solution:
    def minNumberOfFrogs(self, croakOfFrogs: str) -> int:
        # 记录每个字母当前的计数

        #initial 青蛙的数量

        #initial最多青蛙的数量


        #for 循环每一个字母，并计数








        #最后必须保证所有青蛙都完成了"croak"


        #test function
if __name__ == "__main__":
    sol = Solution()
    test_input = "cccccc"
    result = sol.min_Number_Of_Frogs(test_input)
    print(f"input: {test_input} -> output: {result}")