#find top 10 IPs

from collections import Counter

def get_top_ips(file_log, top_n = 10):
    ip_counter = Counter()

with open(file_log, 'r') as f:
    for line in f:
        ip = line.split()[0] #假设第一个字段
        ip_counter += 1

    return ip_counter.most_common(top_n)