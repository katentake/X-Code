class Solution:
    def dominantIndex(self, nums: List[int]) -> int:
        max_val = max(nums)
        max_idx = nums.index(max_val)

        for n in nums:
            # 排除掉最大值自己，检查其他数
            if n != max_val and max_val < n*2:
                return -1

        return max_idx