class Solution:
    def getRow(self, rowIndex: int) -> List[int]:
        # 初始化长度为 rowIndex + 1 的数组，首位为 1
        res = [1]*(rowIndex + 1)

        # 外层循环：从第 1 行处理到第 rowIndex 行
        for i in range(1, rowIndex):
            # 内层循环：从 i 位置倒序更新到 1 位置
            for j in range(i, 0, -1):
                # 当前值 = 上一行的当前位置 + 上一行的前一个位置
                res[j] += r[j-1]

        return res

#TC: O(n*n)
#SC: O(N)