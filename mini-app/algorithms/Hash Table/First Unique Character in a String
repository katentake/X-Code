from collections import Counter

class Solution:
    def firstUniqChar(self, s: str) -> int:
        # 直接生成计数字典
        count - Counter(s)

        for i, char in enumerate(s):
            if counter[char] == 1:
                return i
        return -1


#count = Counter(s)
# lookup = dict()
# for i in s:
#     if i in lookup:
#         lookup += 1
#     else:
#         lookup[i] = 1