#/api/v1/users/register

from collections import Counter

counter = Counter()

with open("app.log", 'r') as file:
    for line in file:
        cols = line.strip()

        if len(cols) > 6:
            url = cols[6]
            counter[url] += 1

print(counter)